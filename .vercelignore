# Vercel auto-detects Vite (`vite build` -> dist/) and runs api/*.py as Python functions. The build needs
# index.html + embed.html + src/ + the configs + api/ + requirements.txt uploaded — everything else is
# local-only tooling or artifacts, kept out here.
server.py
ascii_video.py
videoplayback.html
current.mp4
*.mp4
aws/
docs/
tests/
*.md
__pycache__/
*.pyc
.venv/
.DS_Store
node_modules/
dist/
